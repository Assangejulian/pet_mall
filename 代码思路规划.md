# 代码思路规划

> 宠物商店项目总结 · 写代码前的判断框架

---

## 一、通用 CRUD vs 手写

### 判断流程

```
这个操作涉及几张表？
├── 1 张 → 这个操作有没有条件分支？
│   ├── 无 → ✅ 通用 CRUD
│   └── 有 → ❌ 手写
└── ≥ 2 张 → ❌ 手写
```

### 通用 CRUD 能干的（BaseController）

```java
GET    /{id}       // 单条查询
POST   /           // 新增
PUT    /{id}       // 更新
DELETE /{id}       // 删除
DELETE /batch      // 批量删
GET    /search     // 分页 + 条件搜索
GET    /list       // 全量列表
GET    /by-ids     // 批量查
POST   /batch      // 批量新增
PUT    /batch      // 批量更新
```

### 必须手写的

| 场景 | 例子 | 原因 |
|------|------|------|
| 跨多表事务 | 下单（cart+product+order+item） | 一次改 4 张表 |
| 操作前读当前状态 | 订单状态机 | 先查数据库才知道能不能改 |
| 调外部服务 | AI 对话、发邮件 | 跟数据库无关 |
| 非等值查询 | 附近搜索（经纬度范围） | 不是 where field = value |
| 生成衍生数据 | JWT 登录 | 查完用户还要算 token |

---

## 二、数据校验

### 用 @Valid 做的（输入合法性）

校验的是**请求体本身**，不查数据库：

```java
public class OrderCreateDTO {
    @NotNull                          // 不能为 null
    private Long addressId;

    @NotEmpty                         // 不能为空列表
    private List<OrderItemDTO> items;

    @Data
    public static class OrderItemDTO {
        @NotNull                      // 不能为 null
        private Long productId;

        @Min(1) @Max(999)             // 必须在 1~999 之间
        private Integer quantity;
    }
}
```

**能管的：** 字段 null 不 null、空不空、范围对不对、格式对不对（邮箱/手机号正则）  
**管不了的：** 这个值在业务上下文里合不合理

### 手写校验（业务合法性）

查数据库后才能判断：

```java
// @Valid 管不了这个
Product product = productService.getById(item.getProductId());
if (product == null)                 // 商品存不存在？
    throw new BusinessException(...);
if (product.getStatus() != 1)         // 商品上没上架？
    throw new BusinessException(...);
if (product.getStock() < item.getQuantity()) // 库存够不够？
    throw new BusinessException(...);
```

### 区分标准

```
不依赖数据库的 → @Valid 搞定
依赖数据库的   → 手写，读出来再判断
```

---

## 三、业务决策

### 什么是业务决策

**"先看当前数据是什么，再决定下一步干什么。"**

```java
// 这不是校验，这是决策
if (order.getOrderStatus() == 2 && target == 3) {
    // 已发货 → 允许收货
}
```

通用 CRUD 和 @Valid 都管不了这个，因为：

- 通用 CRUD：只管"把字段更新成新值"，不管"这个值合不合理"
- @Valid：只管"请求里传来的这个字段长啥样"，不管"数据库里那条数据现在啥状态"

### 你们项目的决策例子

| 操作 | 决策规则 | 写在哪 |
|------|----------|--------|
| 订单状态跳转 | `0→1→2→3→4` + 退单分支 | OrderAdminController |
| 商店关闭 | 有关联商品不能关 | StoreController.preUpdate |
| 手机号修改 | 新手机号不能已被注册 | 下单时的地址校验 |
| 商品下架 | 有进行中的订单不下架 | ProductAdminController |

### 通用写法

```java
// ① 先读当前状态
Order order = orderService.getById(id);

// ② 判断能不能做
if (order.getStatus() != 1) {
    throw new BusinessException("当前状态不允许操作");
}

// ③ 执行
order.setStatus(2);
orderService.updateById(order);
```

三步：**读 → 判断 → 写**，缺一不可。

---

## 四、原则速查

| 你想做这个 | 用这个 |
|-----------|--------|
| 字段不能为空、范围对不对 | @Valid |
| 有没有权限操作 | AOP / 拦截器 |
| 这个值在数据库里存不存在 | 手写查库 |
| 当前状态下能不能做这个操作 | 手写 if-else |
| 操作涉及多张表 | 手写 @Transactional |
| 单表无分支的增删改查 | BaseController |