# 宠物商店项目 — 分工方案

> 4人团队 · 2026-06-24 更新 · 基础 CRUD 已完成

---

## 项目概况

```
课程/
├── demo/                  Spring Boot 后端（10表 CRUD 已完成）
├── pet_admin/             Vue3 管理后台（已完成）
├── pet_miniapp/           微信小程序（页面完成，Mock 数据）
├── pet_frontend/          Vue 用户端（风格参考）
├── 业务设计.md
└── 分工.md
```

---

## 前置约定

### BaseController 已提供端点（每个模块自动拥有）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/{id}` | 单条 |
| POST | `/` | 新增 |
| PUT | `/{id}` | 更新 |
| DELETE | `/{id}` | 删除 |
| DELETE | `/batch` | 批量删 |
| GET | `/search` | 分页 + QueryWrapper 条件搜索 |
| GET | `/list` | 全量列表（不过滤） |
| GET | `/by-ids` | 按 ID 批量查 |
| POST | `/batch` | 批量新增 |
| PUT | `/batch` | 批量更新 |

### API 前缀

| 模块 | 前缀 | 补充端点 |
|------|------|---------|
| 用户 | `/api/user` | `/login` `/register` `/info` |
| 地址 | `/api/user/address` | — |
| 店铺 | `/api/store` | `/nearby` |
| 商品 | `/api/product` | — |
| 购物车 | `/api/cart` | — |
| 订单 | `/api/order` | `/create` `/{id}/status` |
| 订单明细 | `/api/order/item` | — |
| 视频 | `/api/video` | `/play/{id}` `/{id}/like` `/{id}/comment` `/{id}/comments` `/upload` |
| 评论 | `/api/comment` | — |
| AI | `/api/ai` | `/chat` `/record` |

### 统一返回

```json
{ "code": 200, "message": "success", "data": ... }
```

---

## 四人分工

```
┌────── 成员A ──────┐  ┌────── 成员B ──────┐  ┌────── 成员C ──────┐  ┌────── 成员D ──────┐
│    用户 + 地址      │  │    商店 + 商品      │  │    视频 + AI        │  │    订单 + 购物车     │
│                    │  │                    │  │                    │  │                     │
│ 登录/注册/资料完善   │  │ 附近搜索逻辑        │  │ 视频列表（含作者）   │  │ 下单 + 状态机流转    │
│ 地址按 user_id 过滤 │  │ 商品多条件筛选      │  │ DeepSeek 对话完善   │  │ 购物车 user_id 过滤  │
│                    │  │                    │  │                    │  │ 退单 / 取消逻辑      │
│ 前端：登录/注册页   │  │ 前端：店铺/商品页   │  │ 前端：首页Feed     │  │ 前端：购物车/订单页  │
│ 个人中心 / 地址管理  │  │ B端：店铺审核/商品  │  │ 视频详情+评论      │  │ B端：订单发货/审核   │
│ B端：用户管理       │  │                    │  │ AI 聊天页          │  │                     │
└────────────────────┘  └────────────────────┘  └────────────────────┘  └────────────────────┘
```

---

### 成员A — 用户 + 地址

**后端**
- `AuthController`：登录校验 + JWT、注册去重、密码加密
- `UserAddressController`：`buildQueryWrapper` 加 `user_id` 过滤
- `GET/PUT /api/user/info`：个人资料

**小程序**
- `pages/user/`：登录 `/api/user/login` → 存 token；个人中心 `/api/user/info`
- `pages/address/`：CRUD 调 `/api/user/address/search`，替换 Mock

**B端**
- 用户管理页：调 `/api/user/search`

---

### 成员B — 商店 + 商品

**后端**
- `StoreController`：加 `GET /api/store/nearby`（经纬度范围查询）
- `ProductController`：`buildQueryWrapper` 加 `store_id`/`product_type`/`category`/`status` 筛选

**小程序**
- `pages/store/`：列表 `/api/store/search`，详情 `/api/store/{id}`
- `pages/detail/`：商品详情 `/api/product/{id}`，替换 Mock

**B端**
- 店铺管理：审核 status 0→1；商品管理：上下架

---

### 成员C — 视频 + AI

**后端**
- `VideoController`：已完成 list（JOIN user）、play、like、comment、upload
- `AiChatController`：DeepSeek 调用 + 上下文记忆 + 记录到 `ai_chat_record`

**小程序**
- `pages/index/`：首页 Feed → `GET /api/video/search`（替换 Mock）
- `pages/video/detail`：详情 `GET /api/video/{id}` + 评论 `/{id}/comments`
- 发评论 `POST /api/video/{id}/comment`，点赞 `POST /api/video/{id}/like`
- `pages/chat/`：`POST /api/ai/chat`

---

### 成员D — 订单 + 购物车

**后端**
- `CartController`：`buildQueryWrapper` 加 `user_id` 过滤
- `PurchaseOrderController`：
  - `POST /api/order` — 创建（cart→order+order_item，减库存）
  - `PUT /api/order/{id}/status` — 状态流转（支付/发货/收货/取消/退单）
  - 状态机校验：0→1→2→3→4，分支 -1/-2/-3/-4

**小程序**
- `pages/cart/`：CRUD 调 `/api/cart/search`，替换 globalData Mock
- `pages/order/`：确认页 `POST /api/order`，列表 `/api/order/search`

**B端**
- 订单管理：发货（2）、退单审核（-2→-3）

---

## 10 张表归属

| 表 | 成员 | 后端重点 |
|-----|:---:|------|
| user | A | login / register / info |
| user_address | A | user_id 过滤 |
| store | B | nearby 搜索 |
| product | B | 多条件筛选 |
| cart | D | user_id 过滤 |
| purchase_order | D | 下单 + 状态机 |
| order_item | D | order_id 查询 |
| video | C | list JOIN + play/like/comment |
| comment | C | video_id 过滤 |
| ai_chat_record | C | DeepSeek 对话记录 |

---

## 开发顺序

| 天 | 内容 |
|----|------|
| 第1天 | 4人并行：各自后端完善 + 小程序对接 API |
| 第2天 | 前后端联调，去 Mock |
| 第3天 | B端页面补齐 + 跨模块联调（下单流程 D→A+B） |
| 第4天 | 演示 PPT + 文档 |

---

## 联调依赖

```
A(用户) ──┐
          ├──▶ D(订单)：下单需要地址 + token
B(商品) ──┘

C(视频) ──▶ B(商品)：视频关联 productId
```