# 宠物商店项目 - 分工方案

## 团队：4人

---

## 前置准备（开工前半天统一完成）

### 1. API 接口约定

| 模块 | 路径前缀     | 示例                                                 |
| ---- | ------------ | ---------------------------------------------------- |
| 用户 | /api/user    | /api/user/login /api/user/register /api/user/address |
| 商店 | /api/store   | /api/store/list /api/store/nearby                    |
| 商品 | /api/product | /api/product/list /api/product/detail/{id}           |
| 订单 | /api/order   | /api/order/create /api/order/status                  |
| 视频 | /api/video   | /api/video/list /api/video/play/{id}                 |
| AI   | /api/ai      | /api/ai/chat                                         |

### 2. 数据库表约定

- 表名：pets_ 前缀区分
- 用户表 sys_user（已有）
- 统一返回体：Result`<T>`（已有）

### 3. 分支约定

- 成员各自开分支：各自的名称 yf    daifu   ...
- 每轮结束合并到 mian

---

## 第一轮：并行开发（4人同时）

┌──── 成员A ─────┐ ┌──── 成员B ─────┐ ┌──── 成员C ─────┐ ┌──── 成员D ─────┐
│   用户模块       │ │   商店模块       │ │   AI智能问答     │ │   视频模块       │
│ 登录/注册       │ │ 商店CRUD        │ │ DeepSeek对话     │ │ 视频列表/播放   │
│ 个人中心        │ │ 地图/附近搜索    │ │ 客服/推荐/宠物   │ │ 视频上传管理    │
│ 收货地址CRUD    │ │ 商店详情        │ │                  │ │ 视频链接商品    │
└─────────────────┘ └─────────────────┘ └─────────────────┘ └─────────────────┘

### 成员A — 用户模块

- 【后端】完善 UserController：注册、登录、个人信息修改
- 【后端】新建 AddressController：收货地址CRUD（多地址+默认）
- 【后端】会员管理：用户列表查看
- 【后端】现有 User.java 补充字段（会员等级、头像等）
- 【前端】登录页 / 注册页 / 个人中心页 / 地址管理页

### 成员B — 商店模块

- 【后端】新建 StoreController：商店CRUD
- 【后端】新建 NearByController：附近商店搜索（含地理位置字段）
- 【后端】地图API对接（高德/百度，获取位置+导航）
- 【前端】附近商店地图页 / 商店详情页 / 后台商店管理页

### 成员C — AI智能问答

- 【后端】新建 AIChatController：调用DeepSeek API（Spring AI已有依赖）
- 【后端】新建 AIService：对话管理、上下文记忆、推荐逻辑
- 【后端】宠物知识库构建
- 【前端】智能问答聊天页面（类似客服对话框）

### 成员D — 视频模块

- 【后端】新建 VideoController：视频列表、播放、上传
- 【后端】新建 VideoService：文件存储、视频关联商品
- 【前端】视频列表页 / 视频播放页

### 第一轮解耦方式

- 各模块 Mock 数据自测，不等人
- 互相依赖的接口（如用户token）用 Mock 先跑通
- 视频链接商品：先留 productId 字段，第二轮补上

---

## 第二轮：合兵开发（2组并行）

┌───── 成员A + 成员B ─────┐ ┌───── 成员C + 成员D ─────┐
│       商品模块             │ │       订单模块             │
│ 商品CRUD/分类              │ │ 购物车                     │
│ 首页展示策略（加分）        │ │ 订单状态机（0→1→2→3→4）   │
│                            │ │ 退单流程（-1/-2/-3/-4）    │
│                            │ │ 会员价格策略（加分）        │
└────────────────────────────┘ └────────────────────────────┘

### 成员A + 成员B — 商品模块

- 【后端】新建 ProductController：商品CRUD、分类（宠物唯一/周边不限量）
- 【后端】关联商店（StoreId）
- 【前端】商品列表页 / 商品详情页 / 后台商品管理页
- 【加分】首页商品展示策略、视频关联商品

### 成员C + 成员D — 订单模块

- 【后端】新建 CartController：购物车（加入/修改/删除）
- 【后端】新建 OrderController：下单、支付、发货、收货
- 【后端】完整订单状态机：
  - 0-已下单 → 1-已支付 → 2-已发货 → 3-已收货 → 4-已评价（结束）
  - -1-取消订单（0→1间可取消）
  - -2-申请退单（2→3间可申请）
  - -3-退单审核通过（结束）
  - -4-管理员直接退单
- 【后端】订单管理：发货、退单审核
- 【前端】购物车页 / 订单确认页 / 订单列表页 / 订单详情页
- 【加分】会员等级动态定价

---

## 第三轮：集成收尾

四人一起：

- 前后端联调，去掉 Mock 换成真实接口
- 选做加分：消息推送 / 统计数据可视化 / 指纹人脸登录
- 代码扫描修复（SAST + APPScan）
- 演示PPT + 文档

---

## 进度时间线

| 周次      | 内容                          |
| --------- | ----------------------------- |
| 第1周前半 | 定API+数据库 + 第一轮并行开发 |
| 第1周后半 | 第一轮完成 + 第二轮并行开发   |
| 第2周前半 | 第二轮完成                    |
| 第2周后半 | 集成联调 + 代码扫描修复       |
| 第3周     | 演示PPT + 文档收尾            |

| 成员 | 第一轮 | 第二轮 |
|------|--------|--------|
| A | 用户模块 | 商品模块（+B） |
| B | 商店模块 | 商品模块（+A） |
| C | AI智能问答 | 订单模块（+D） |
| D | 视频模块 | 订单模块（+C） |

## 数据库结构 → 详见 doc/database.sql

| 表名 | 负责模块 | 说明 |
|------|----------|------|
| user（已有） | 用户模块 | 扩展 member_level/real_name/birthday |
| user_address | 用户模块 | 收货地址，多地址+默认 |
| store | 商店模块 | 含经纬度，地图搜索用 |
| product | 商品模块 | type: 1-宠物 2-周边，关联 store |
| cart | 订单模块 | 购物车，用户+商品唯一 |
| order_info | 订单模块 | 订单主表，含完整状态字段 |
| order_item | 订单模块 | 订单明细，含评价字段 |
| video | 视频模块 | 视频，关联 product |
| ai_chat_record | AI模块 | 对话记录（可选） |

### 各模块建表语句归属

- 成员A 执行：user(ALTER) + user_address
- 成员B 执行：store
- 成员C 执行：ai_chat_record
- 成员D 执行：video
- 第二轮 A+B 执行：product
- 第二轮 C+D 执行：cart + order_info + order_item

## 后端架构（按业务模块分）

`
demo/src/main/java/com/pat/demo/
├── common/                    公共基础（已完成）
│   ├── config/               MvcConfig, Knife4j, Redis, MybatisPlus...
│   ├── domain/               Result, BaseEntity, LogicDeleteEntity, ErrorCode
│   ├── exception/            BusinessException, GlobalExceptionHander
│   ├── interceptor/          LoginInterceptor, RefreshTokenInterceptor
│   └── utils/                UserHolder, TokenUtils, MailUtils
│
├── user/                      用户模块（示例完成）
│   ├── controller/
│   ├── service/
│   ├── mapper/
│   ├── entity/
│   ├── dto/
│   └── vo/
│
├── store/                     商店模块（空，待开发）
├── product/                   商品模块（空，待开发）
├── order/                     订单模块（空，待开发）
├── video/                     视频模块（空，待开发）
└── ai/                        AI模块（空，待开发）
`

### 模块约定
- 每个模块内部自包含 controller/service/mapper/entity/dto/vo
- 模块间通过 common 统一返回体 Result<T> 通信
- API前缀：/api/user、/api/store、/api/product、/api/order、/api/video、/api/ai
- Token认证：common拦截器统一处理，模块不用管
