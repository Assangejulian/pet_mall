# 宠物商店项目 — 分工方案

> 4人团队 · 2026-06-24 更新 · 基础 CRUD 已完成

---

## 项目概况

```
课程/
├── demo/                  Spring Boot 后端（10表 CRUD 已完成）
├── pet_admin/             Vue3 管理后台（已完成）
├── pet_miniapp/           微信小程序（页面完成，Mock 数据）
├── pet_frontend/          Vue 用户端（风格参考）
├── 业务设计.md
└── 分工.md
```

---

## 前置约定

### API 前缀

| 模块 | 前缀 | 已有端点 |
|------|------|---------|
| 用户 | `/api/user` | login, register, /address CRUD |
| 店铺 | `/api/store` | BaseController CRUD |
| 商品 | `/api/product` | BaseController CRUD |
| 购物车 | `/api/cart` | BaseController CRUD |
| 订单 | `/api/order` | BaseController CRUD |
| 订单明细 | `/api/order/item` | BaseController CRUD |
| 视频 | `/api/video` | BaseController + list/play/like/comment/upload |
| 评论 | `/api/comment` | BaseController CRUD |
| AI | `/api/ai` | chat, /record CRUD |

### 统一返回体

```json
{ "code": 200, "message": "success", "data": ... }
```

### 数据库

`demo/src/main/resources/doc/pet_store.sql` — 10 张表，雪花 ID，DATETIME(3)

---

## 四人分工

```
┌────── 成员A ──────┐  ┌────── 成员B ──────┐  ┌────── 成员C ──────┐  ┌────── 成员D ──────┐
│    用户 + 地址      │  │    商店 + 商品      │  │    视频 + AI        │  │    订单 + 购物车     │
│                    │  │                    │  │                    │  │                     │
│ 后端：完善登录注册   │  │ 后端：附近搜索逻辑   │  │ 后端：视频列表增强   │  │ 后端：下单 + 状态机   │
│ 个人资料 / 地址过滤  │  │ 商品分类筛选 / 下架  │  │ DeepSeek 对话完善   │  │ 购物车 user_id 过滤  │
│                    │  │                    │  │                    │  │ 退单 / 取消逻辑      │
│ 前端：登录/注册页   │  │ 前端：店铺列表/详情  │  │ 前端：首页Feed对接  │  │ 前端：购物车页对接   │
│ 个人中心 / 地址管理  │  │ 商品列表 / 详情页   │  │ 视频详情+评论对接   │  │ 订单列表/详情/确认   │
│                    │  │ 后台店铺/商品管理   │  │ AI 聊天页对接      │  │ 后台订单管理         │
└────────────────────┘  └────────────────────┘  └────────────────────┘  └────────────────────┘
```

---

### 成员A — 用户 + 地址

**后端（`user/` 包）**
- `AuthController`：完善登录校验（JWT）、注册去重、密码加密
- `UserAddressController`：重写 `buildQueryWrapper`，按当前登录用户 `user_id` 过滤
- 用户资料：GET/PUT `/api/user/info`

**前端（小程序 `pages/user/` + `pages/address/`）**
- 登录页：调 `/api/user/login`，存 token
- 注册页：调 `/api/user/register`
- 个人中心：调 `/api/user/info` 展示 + 修改
- 地址管理：调 `/api/user/address` CRUD，替换 Mock

**前端（B端 `pet_admin`）**
- 用户管理页：对接用户列表 API

---

### 成员B — 商店 + 商品

**后端（`store/` + `product/` 包）**
- `StoreController`：加 `GET /api/store/nearby`（经纬度范围查询）
- `ProductController`：重写 `buildQueryWrapper`，按 `store_id`/`product_type`/`category`/`status` 筛选
- 商品下架：status 改为 0

**前端（小程序 `pages/store/` + `pages/detail/`）**
- 店铺列表：调 `/api/store/list`
- 店铺详情：调 `/api/store/{id}`
- 商品详情页：调 `/api/product/{id}`，替换 Mock

**前端（B端 `pet_admin`）**
- 店铺管理：审核 status 0→1
- 商品管理：上下架、列表

---

### 成员C — 视频 + AI

**后端（`video/` + `ai/` 包）**
- `VideoController`：已有 list（含作者 JOIN）、play、like、comment、upload
- `AiChatController`：完善 DeepSeek 调用，上下文记忆，记录到 `ai_chat_record`

**前端（小程序 `pages/index/` + `pages/video/` + `pages/chat/`）**
- 首页 Feed：调 `GET /api/video/list`，替换 Mock 数据
- 视频详情：调 `GET /api/video/{id}` + `GET /api/video/{id}/comments`
- 发评论：调 `POST /api/video/{id}/comment`
- 点赞：调 `POST /api/video/{id}/like`
- AI 聊天：调 `POST /api/ai/chat`，对接 DeepSeek

---

### 成员D — 订单 + 购物车

**后端（`order/` 包）**
- `CartController`：重写 `buildQueryWrapper`，按 `user_id` 过滤
- `PurchaseOrderController`：
  - `POST /api/order` 创建订单（cart → order + order_item，减库存）
  - `PUT /api/order/{id}/status` 状态流转（支付/发货/收货/取消/退单）
  - 状态机校验（0→1→2→3→4，分支 -1/-2/-3/-4）
- `OrderItemController`：按 `order_id` 查询明细

**前端（小程序 `pages/cart/` + `pages/order/`）**
- 购物车：调 `/api/cart` CRUD，替换 globalData Mock
- 订单确认：选地址 → 调 `POST /api/order` 创建
- 订单列表：调 `GET /api/order/list`
- 订单详情：调 `GET /api/order/{id}`

**前端（B端 `pet_admin`）**
- 订单管理：发货、退单审核

---

## 10 张表归属

| 表 | 成员 | 后端重点 |
|-----|:---:|------|
| user | A | login/register/info |
| user_address | A | user_id 过滤 |
| store | B | nearby 搜索 |
| product | B | 多条件筛选 |
| cart | D | user_id 过滤 |
| purchase_order | D | 下单 + 状态机 |
| order_item | D | order_id 查询 |
| video | C | list JOIN + play/like/comment |
| comment | C | video_id 过滤 |
| ai_chat_record | C | DeepSeek 对话记录 |

---

## 开发顺序

| 阶段 | 内容 | 依赖 |
|------|------|------|
| **第1天** | 4人并行：各自后端完善 + 小程序对接 | 无（CRUD 基础已有） |
| **第2天** | 前后端联调，去 Mock | 各自模块对接完 |
| **第3天** | B端页面补齐 + 跨模块联调（下单流程） | D 订单依赖 A 地址 + B 商品 |
| **第4天** | 演示 PPT + 文档 | — |

---

## 联调依赖

```
A(用户) ──┐
          ├──▶ D(订单)：下单需要地址 + 用户 token
B(商品) ──┘

C(视频) ──▶ B(商品)：视频关联 productId

B(商品) ──▶ C(视频)：商品详情关联视频
```