<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pat.report.mapper.ReportMapper">

    <!-- 查询销量排名 Top10 -->
    <select id="selectSalesTop10" resultType="java.util.Map">
        SELECT od.product_name AS name, SUM(od.quantity) AS number
        FROM order_item od
        INNER JOIN purchase_order o ON od.order_id = o.id
        WHERE o.order_status IN (3, 4)
        <if test="beginTime != null">
            AND o.pay_time &gt;= #{beginTime}
        </if>
        <if test="endTime != null">
            AND o.pay_time &lt;= #{endTime}
        </if>
        GROUP BY od.product_name
        ORDER BY number DESC
        LIMIT 0, 10
    </select>

</mapper>
